#!/bin/bash

layout=$(xkb-switch)

case "$layout" in
    us)
        echo "<span color='#9397dc'> </span>US"
        ;;
    ara)
        echo "<span color='#9397dc'> </span>AR"
        ;;
    *)
        echo "$layout"
        echo "$layout"
        ;;
esac

