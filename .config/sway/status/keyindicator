#!/bin/bash

color="#d3c6aa"
layout=$(swaymsg -t get_inputs | grep xkb_active_layout_name | head -n1 | awk -F'"' '{print $4}')

if [ "$layout" = "English (US)" ]; then
    layout="us"
elif [ "$layout" = "Arabic" ]; then
    layout="ar"
fi

echo "<span foreground='$color'>Key</span> $layout"

